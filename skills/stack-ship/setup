#!/usr/bin/env bash
# Idempotent pack setup, run by AIBootstrap's install_pack on every bootstrap.
# No dependencies to install; validates the pack instead.
set -u
cd "$(dirname "$0")"
exec bash tests/gate.sh

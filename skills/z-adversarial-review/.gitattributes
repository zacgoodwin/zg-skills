# Golden fixtures are compared byte-for-byte against constructor output; a
# core.autocrlf=true checkout must not smudge them to CRLF (found by the
# adversarial review of PR #2: 2/99 tests failed in a fresh worktree).
tests/fixtures/*.txt -text
